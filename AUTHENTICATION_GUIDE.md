# دليل نظام المصادقة والتفويض
# Trade For Egypt - Authentication & Authorization Guide

## نظرة عامة

تم تطوير نظام مصادقة وتفويض شامل (RBAC - Role-Based Access Control) لتطبيق Trade For Egypt. يسمح هذا النظام بـ:

- **تسجيل دخول آمن** باستخدام البريد الإلكتروني وكلمة المرور
- **إدارة الأدوار (Roles)** - 8 أدوار مختلفة مع صلاحيات محددة
- **حماية المسارات** - كل قسم متاح فقط للأدوار المصرح لها
- **لوحة تحكم المسؤول** - إدارة المستخدمين والأدوار

---

## الخطوات الأولية

### 1. تطبيق تحديثات قاعدة البيانات (Supabase SQL)

**الملف:** `auth_setup.sql`

1. اذهب إلى [Supabase Dashboard](https://app.supabase.com)
2. اختر مشروع `tradeforegypt`
3. اذهب إلى **SQL Editor**
4. انسخ محتوى ملف `auth_setup.sql` كاملاً
5. الصق الكود في محرر SQL واضغط **Run**

**ما يفعله هذا الكود:**
- ينشئ نوع بيانات للأدوار (`user_role`)
- ينشئ جدول `user_profiles` لربط المستخدمين بالأدوار
- يفعّل حماية مستوى الصف (RLS)
- ينشئ سياسات وصول محددة لكل دور
- ينشئ دوال تلقائية لإنشاء ملفات تعريف جديدة

### 2. تحديث الكود البرمجي

```bash
cd tradeforegypt
npm install
# أو
pnpm install
```

---

## الأدوار المتاحة

| الدور | الرمز | الأقسام المتاحة |
|------|------|-----------------|
| **مسؤول (Admin)** | `admin` | جميع الأقسام |
| **كول سنتر** | `call_center` | لوحة التحكم، الكول سنتر، العملاء، الاستقبال |
| **متابعة** | `follow_up` | لوحة التحكم، المتابعة، العملاء، استقبال الأجهزة |
| **مبيعات** | `sales` | لوحة التحكم، المبيعات، العملاء، الفواتير |
| **صيانة** | `maintenance` | لوحة التحكم، الخدمات، المخزون، استقبال الأجهزة |
| **استقبال** | `reception` | لوحة التحكم، الاستقبال، استقبال الأجهزة، العملاء |
| **موارد بشرية** | `hr` | لوحة التحكم، الموظفين، الرواتب، الحوافز |
| **مالية** | `finance` | لوحة التحكم، الفواتير، الحسابات، المصروفات، التقارير |

---

## كيفية استخدام النظام

### تسجيل الدخول

1. اذهب إلى صفحة تسجيل الدخول: `https://your-app.com/login`
2. أدخل بريدك الإلكتروني وكلمة المرور
3. اضغط **تسجيل الدخول**
4. سيتم توجيهك إلى لوحة التحكم

### إنشاء مستخدم جديد

**للمسؤول فقط:**

1. اذهب إلى **إدارة المستخدمين** (Admin Panel)
2. اضغط **إضافة مستخدم جديد**
3. أدخل البريد الإلكتروني والاسم
4. حدد الدور المناسب
5. اضغط **حفظ**

سيتم إرسال بريد تأكيد للمستخدم الجديد يحتوي على رابط لتعيين كلمة المرور.

### تعديل دور المستخدم

**للمسؤول فقط:**

1. اذهب إلى **إدارة المستخدمين**
2. ابحث عن المستخدم المطلوب
3. اضغط على أيقونة **التعديل** (Edit)
4. اختر الدور الجديد من القائمة المنسدلة
5. اضغط **حفظ التغييرات**

### تعطيل/تفعيل مستخدم

**للمسؤول فقط:**

1. اذهب إلى **إدارة المستخدمين**
2. اضغط على أيقونة **الحالة** (Status) بجانب المستخدم
3. سيتم تبديل حالة المستخدم بين نشط/معطل

---

## الملفات المضافة/المعدلة

### ملفات جديدة:

1. **`src/lib/auth.ts`** - مكتبة المصادقة والتفويض
2. **`src/app/login/page.tsx`** - صفحة تسجيل الدخول
3. **`src/app/admin/page.tsx`** - لوحة تحكم المسؤول
4. **`src/middleware.ts`** - حماية المسارات
5. **`auth_setup.sql`** - سكريبت قاعدة البيانات

### ملفات معدلة:

1. **`src/components/Sidebar.tsx`** - تحديث القائمة الجانبية لعرض الأقسام المتاحة فقط
2. **`package.json`** - إضافة مكتبة `@supabase/auth-helpers-nextjs`

---

## المتغيرات البيئية المطلوبة

تأكد من أن ملف `.env.local` يحتوي على:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

---

## كيفية استخدام مكتبة المصادقة في الكود

### الحصول على المستخدم الحالي

```typescript
import { getCurrentUser, getCurrentUserProfile, getUserRole } from '@/lib/auth'

// الحصول على بيانات المستخدم من Supabase Auth
const user = await getCurrentUser()

// الحصول على ملف التعريف الكامل (مع الدور)
const profile = await getCurrentUserProfile()

// الحصول على الدور فقط
const role = await getUserRole()
```

### التحقق من الصلاحيات

```typescript
import { hasAccessToSection } from '@/lib/auth'

// التحقق من دور واحد
const canAccess = await hasAccessToSection('admin')

// التحقق من عدة أدوار
const canAccess = await hasAccessToSection(['admin', 'hr'])
```

### الحصول على الأقسام المتاحة

```typescript
import { getAvailableSections } from '@/lib/auth'

const sections = await getAvailableSections()
// مثال: ['dashboard', 'call-center', 'customers', 'reception']
```

### تسجيل الدخول والخروج

```typescript
import { signIn, signOut } from '@/lib/auth'

// تسجيل الدخول
await signIn('user@example.com', 'password123')

// تسجيل الخروج
await signOut()
```

---

## معالجة الأخطاء الشائعة

### "فشل تسجيل الدخول"

**الأسباب المحتملة:**
- البريد الإلكتروني أو كلمة المرور غير صحيحة
- المستخدم معطل (is_active = false)
- المستخدم لم يتم إنشاؤه بعد

**الحل:**
- تحقق من بيانات الدخول
- اطلب من المسؤول التحقق من حالة المستخدم

### "وصول مرفوض"

**الأسباب المحتملة:**
- دورك لا يسمح بالوصول إلى هذا القسم
- لم تقم بتسجيل الدخول

**الحل:**
- اطلب من المسؤول تحديث دورك
- تأكد من تسجيل دخولك

### "خطأ في جلب البيانات"

**الأسباب المحتملة:**
- مشكلة في الاتصال بـ Supabase
- مفاتيح API غير صحيحة

**الحل:**
- تحقق من متغيرات البيئة
- أعد تحميل الصفحة
- تحقق من حالة Supabase

---

## الخطوات التالية

### 1. اختبار النظام

```bash
npm run dev
# أو
pnpm dev
```

ثم اذهب إلى `http://localhost:3000/login`

### 2. إنشاء حسابات اختبار

استخدم Supabase Auth لإنشاء حسابات اختبار بأدوار مختلفة.

### 3. اختبار الصلاحيات

- سجل دخول بحسابات مختلفة
- تحقق من أن كل دور يرى فقط الأقسام المتاحة له

### 4. نشر التطبيق

```bash
npm run build
npm run start
```

---

## ملاحظات أمان مهمة

⚠️ **لا تنسى:**

1. **لا تشارك مفاتيح API** - احفظها في متغيرات البيئة فقط
2. **استخدم HTTPS** - تأكد من أن التطبيق يعمل على HTTPS في الإنتاج
3. **قم بتحديث كلمات المرور بانتظام** - شجع المستخدمين على تغيير كلمات مرورهم
4. **راقب السجلات** - تحقق من سجلات Supabase بحثاً عن أنشطة مريبة
5. **استخدم دور Admin بحذر** - قلل عدد المسؤولين قدر الإمكان

---

## الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تحقق من [توثيق Supabase](https://supabase.com/docs)
2. تحقق من [توثيق Next.js](https://nextjs.org/docs)
3. تحقق من سجلات المتصفح (Console)
4. تحقق من سجلات Supabase (Logs)

---

**آخر تحديث:** 2026-02-20
**الإصدار:** 1.0.0
